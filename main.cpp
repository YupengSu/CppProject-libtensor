#include <algorithm>
#include <ios>
#include <vector>

#include "tensor.hpp"

using namespace ts;

int main() {
    Tensor<int> a = Tensor<int>({{2, 2}});
    Tensor<float> b = Tensor<float>({{2, 3.2}});

}